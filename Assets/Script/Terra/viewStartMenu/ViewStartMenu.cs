using System.Collections;
using System.Collections.Generic;

public class ViewStartMenu 
{
    /*
    public Button StartButton;

    // Start is called before the first frame update
    void Start()
    {
        StartButton.onClick.AddListener(() => StartButtonMethod(StartButton));
    }
    void StartButtonMethod(Button buttonPressed) {

            ControllerButton.EventCall(ControllerConstant.StartBattle, ControllerConstant.StartBattle, null);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
