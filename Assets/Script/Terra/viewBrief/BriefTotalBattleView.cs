using System.Collections;
using System.Collections.Generic;

public class BriefTotalBattleView 
{
    /*
    public Button CloseButton;
    public Text TextTypeWriter;
    public Image ImageBrief;
    public List<Sprite> ImageBriefList;
    void Start()
    {
        MockGlobalPlanet mockGlobalPlanet = new MockGlobalPlanet(false);

        CloseButton.onClick.AddListener(() => CloseButtonMethod(CloseButton));

        Debug.Log(" =  SelectHero  =     =Scenari =commandStrate ist   = " + BattlePlanetModel.VictoryScenario.Scenario);
        print( "=== SetSelectAt  o ====" + BattlePlanetModel.VictoryScenario.Scenario.ImageMission());
        StartCoroutine(PrintTypeWriter(BattlePlanetModel.VictoryScenario.Scenario.GetMission()));
        //ImageBrief
        ImageBrief.sprite = ImageBriefList[BattlePlanetModel.VictoryScenario.Scenario.ImageMission()];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    void CloseButtonMethod(Button buttonPressed)
    {
        

        ControllerButton.EventCall(ControllerConstant.CloseTotalMenu, ControllerConstant.CloseTotalMenu, null);
    }
    IEnumerator PrintTypeWriter(string Message)
    {
        foreach (var item in Message)
        {
            TextTypeWriter.text += item;
            yield return new WaitForSeconds(0.1f);
        }

    }
    */
}
