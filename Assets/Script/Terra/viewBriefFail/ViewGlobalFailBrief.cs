using System.Collections;
using System.Collections.Generic;

public class ViewGlobalFailBrief 
{
    /*
    public Button CloseButton;

    void Start()
    {
        CloseButton.onClick.AddListener(() => CloseButtonMethod(CloseButton));
    }

    void CloseButtonMethod(Button buttonPressed)
    {

        ControllerButton.EventCall(ControllerConstant.StartMenuInit, ControllerConstant.StartMenuInit, null);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
