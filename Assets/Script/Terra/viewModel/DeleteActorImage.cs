using System.Collections;
using System.Collections.Generic;

public class DeleteActorImage
{
	/*
	public static List<GameObject> DeleteActor(List<GameObject> heroImage_ar)
	{
		if (heroImage_ar != null)
		{
			foreach (GameObject heroImage in heroImage_ar)
			{

				//DeleteActorButton.DeleteActorTrue(stage, heroImage);
			}
		}
		heroImage_ar = new List<GameObject>();
		return heroImage_ar;
	}
	public static List<GameObject> GetActorWithName(List<GameObject> heroImage_ar, int Id)
	{
		List<GameObject> actor_ar = new List<GameObject>();
		if (heroImage_ar != null)
		{

			foreach (GameObject care in heroImage_ar)
			{
				if (care != null)
				{
					var tileSandScript = care.GetComponent<TileSand>();

					if (tileSandScript != null)
					{

						if (tileSandScript.IdTileSand == Id)
						{
							actor_ar.Add(care);

						}
					}
				}
			}

		}
		return actor_ar;
	}
	public static List<GameObject> DeleteHero(List<GameObject> heroImage_ar)
	{
		foreach (GameObject care in heroImage_ar)
		{
		
			//DeleteActorButton.DeleteActor(stage, care);
		}
		heroImage_ar = new List<GameObject>();
		return heroImage_ar;
	}
	*/
}