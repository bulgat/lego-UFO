using System.Collections;
using System.Collections.Generic;


public class UnitInfo 
{
    /*
    public GameObject AnimUnitView;

    public int ImageId;
    public int UnitId;
    public GameObject Explode;
    public GameObject Infantery;
    public GameObject MainUnit;
    public int Damage;
    private bool _Dead;
    private float _countUpDead = 0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (_Dead)
        {
            _countUpDead += 2.0f;
        }
    }
    public void SetAttackInfantry()
    {
        AnimUnitView animUnitView = AnimUnitView.GetComponent<AnimUnitView>();
        animUnitView.SetAttackInfantry();
    }
    public void SetDeadInfantry()
    {
        AnimUnitView animUnitView = AnimUnitView.GetComponent<AnimUnitView>();
        animUnitView.SetDeadInfantry();
        //anim.SetInteger("situation", 2);
        _Dead = true;
    }
    void OnGUI()
    {
        if (_Dead) {
           // RectTransform rt = (RectTransform)Infantery.transform;


            GUI.contentColor = Color.red;
        
            Vector3 screenCoord = Camera.main.WorldToScreenPoint(transform.position);
            //screenCoord = new Vector3(screenCoord.x- rt.rect.height, screenCoord.y);
            
            //GUI.Label(new Rect(screenCoord.x, screenCoord.y-1+(Camera.main.orthographicSize- _countUpDead), 100, 20), ""+ Damage);
            
            //Vector3 screenCoord = Camera.main.WorldToScreenPoint(transform.position);
            GUI.Label(new Rect(screenCoord.x, screenCoord.y - 1 + (Camera.main.orthographicSize * 37 - _countUpDead), 100, 20), "" + Damage);
            
         }
    }
    */
}
