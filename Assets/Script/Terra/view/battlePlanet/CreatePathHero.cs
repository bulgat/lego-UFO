using System.Collections;
using System.Collections.Generic;


public class CreatePathHero 
{
	/*
	private List<GameObject> _heroPathImage_ar;

	public CreatePathHero()
	{
		_heroPathImage_ar = new List<GameObject>();
	}
	public List<ButtonEvent>  DrawPath(List<GameObject> stageList, 
			List<ButtonEvent> buttonEvent_ar,
			float SlipX, float SlipY,
			int widthTile, int heightTile)
	{
		


		_heroPathImage_ar = DeleteActorImage.DeleteHero(_heroPathImage_ar);
		if (buttonEvent_ar != null)
		{
			foreach (ButtonEvent buttonEvent in buttonEvent_ar)
			{

				Texture tileImage;
				bool imageAttack = buttonEvent.NameEvent.Equals(ControllerConstant.AttackHero) ||
						buttonEvent.NameEvent.Equals(ControllerConstant.SeaAttackHero);
				if (imageAttack)
				{
					//tileImage = loadBibleImage.GetLoadImage(GraficBibleConstant.SelectAttack);
					//widthTile =heightTile;
				}
				else
				{
					//tileImage = loadBibleImage.GetLoadImage(GraficBibleConstant.SelectFon);
				}

			
			}
		}
		return buttonEvent_ar;
	}
	public void DeleteDrawPath()
	{
		DeleteActorImage.DeleteHero( _heroPathImage_ar);
	}
*/
}
